/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package directorio;

/**
 *
 * @author Usuario
 */
public class Monitor {
    /*
    
    while(true)
    {
        lee del socket
        switch(comando)
    {
    case REGISTRO:     
            escucha registro de servidores y le pide al directorio q lo agregue a la lista en estado pendiente 
            si no es el primero, le pide al directorio q le devuelva el servidor con menos carga y le avisa a ese servido
            q haga sincronizacion total con el q recien se registra
    case LISTO?:
            le dice al directorio q lo marque como listo y lo muestre por pantalla
    case PING:
             pide la lista de servs al directorio y haciendole ping echo a los servidores y si se cae uno le pide al directorio q lo elimine
    }
    
    */ 
}
